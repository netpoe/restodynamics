import { DocumentNode } from 'graphql';
import { OperationResult } from '../types';
import { CombinedError } from '../utils';
export interface UseMutationState<T> {
    fetching: boolean;
    data?: T;
    error?: CombinedError;
    extensions?: Record<string, any>;
}
export declare type UseMutationResponse<T, V> = [UseMutationState<T>, (variables?: V) => Promise<OperationResult<T>>];
export declare const useMutation: <T = any, V = object>(query: string | DocumentNode) => [UseMutationState<T>, (variables?: V | undefined) => Promise<OperationResult<T>>];
