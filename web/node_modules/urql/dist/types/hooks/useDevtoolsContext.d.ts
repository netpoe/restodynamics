import { OperationContext } from '../types';
/** Creates additional context values for serving metadata to devtools. */
export declare const useDevtoolsContext: () => Partial<OperationContext> | undefined;
